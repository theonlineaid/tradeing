import FontSizeControl from '#common/components/FontSizeControl'

const MoversGainersSettings = ({checkData, handleCheckData, tableName}) => {
  return <FontSizeControl tableName={tableName} />
}

export default MoversGainersSettings
